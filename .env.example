# Durable local SQLite database. Relative paths resolve from the project root.
DATABASE_PATH=.data/index-lens.sqlite

# Optional: override the default 24-hour source cache.
HOLDINGS_CACHE_TTL_SECONDS=86400
